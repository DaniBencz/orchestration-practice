apiVersion: v1
kind: Service
metadata:
  name: ingress
  # namespace: kiskutya
spec:
  # type defaults to ClusterIP
  type: LoadBalancer
  selector:
    # calls pods, not deployments!
    app: api
  ports:
    - port: 4000
      targetPort: 80
